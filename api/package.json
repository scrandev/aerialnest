{
  "name": "aerialnest-api",
  "version": "1.0.0",
  "description": "Aerial Nest backend API",
  "main": "src/index.js",
  "scripts": {
    "build": "zip -r lambda-code.zip src/",
    "deploy": "npm run build && aws s3 cp lambda-code.zip s3://aerialnest-deployment-code/api/"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "jsonwebtoken": "^9.0.2"
  }
}
